[www]
; Keep the container environment (DATABASE_URL, START_GG_*, APP_ENV, …) in the
; worker processes so the detached background import worker spawned via the web
; request inherits it.
clear_env = no
